name: schedule-check

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: run script
        run: node check.js
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
